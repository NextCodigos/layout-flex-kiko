// Defining a constant variable 'nombre' with the value "Yandrak"
const nombre ="Yandrak"

// Defining an object 'persona' with properties 'id', 'nombre', and 'trabajo'
const persona = {    
    id: 1,
    nombre: "Yadrak", // Typo: should be "Yandrak" to match the constant above
    trabajo: "Frontend developer"
}

// Logging the value of 'nombre' to the console
console.log("Nombre: " + nombre);

// Logging the object 'nombre' to the console
console.log({nombre})

// Logging the object 'persona' to the console
console.log({persona})
